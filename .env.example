# Root Environment Variables
# This file is for reference. Actual environment variables should be set in:
# - server/.env for backend configuration
# - client/.env.local for frontend configuration
# - Vercel dashboard for production deployments

# Client Configuration (Next.js)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Server Configuration (Express)
PORT=8000
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database_name
JWT_SECRET=your-jwt-secret
REFRESH_TOKEN_SECRET=your-refresh-token-secret
NODE_ENV=development
